import {useState, useEffect} from "react";
import Box from "@mui/material/Box";
import List from "@mui/material/List";
import ListItem from "@mui/material/ListItem";
import ListItemButton from "@mui/material/ListItemButton";
import ListItemText from "@mui/material/ListItemText";

const Navigation=(props)=> {
	return (
		<Box sx={{width: "50%", maxWidth: 250}}>
			<nav>
				<List>
					<NavLink text="Tema" href="#tema" accesskey="t"/>
					<NavLink text="Resumo" href="#resumo" accesskey="r"/>
					<NavLink text="Problema" href="#problema" accesskey="p"/>
					<NavLink text="Justificativa" href="#justificativa" accesskey="j"/>
					<NavLink text="Objetivos" href="#objetivos" accesskey="o"/>
					<NavLink text="Referencial teórico (abre em uma nova guia)" href="referencial_teorico.html" accesskey="t"/>
					<NavLink text="Metodologia" href="#metodologia" accesskey="m"/>
					<NavLink text="Conclusões parciais" href="#conclusoes" accesskey="c"/>
					<NavLink text="Referências (abre em uma nova guia)" href="referencias.html" accesskey="f"/>
				</List>
			</nav>
		</Box>
	);
}
export default Navigation;